1. Boid array renderable. Filled in JS with junk data
2. Boids movable in compute shader. Just move linearly in some direction
3. Boids actually boiding? 

3.1. Cleanup!

3.5. Does removing ping-pong impact anything?

3.52. Instance numbers!

3.55. Colors. Figure out how fragment shader knows which boid it is. Color appropriately!
    a. Interstage variables!

3.6. Possible fun:
    a. Obstacles?
    b. Mouse attract / repel?
    c. Different speeds / sizes / shapes?

3.65. More tuning?

3.7. Improvements
    a. Something something workgroups?

4. Spatial partitioning - 3 compute shaders!
    a. Parallel: compute gridLoc for each boid. 
        i. Use atomic adds to count #s in each gridLoc
    b. One worker(?):
        i. Compute initial offsets for all gridLocs based off of counts
        ii. Have count for each gridLoc of how many have been placed
        iii. Loop over all boids, placing each in the next open space offset from their gridLoc
        iv. Can this be done more in parallel?
    c. Parallel: Each boid will know their own gridLoc offset AND how many boids are there
        i. Only loop over that section of the gridLoc (and adjacent (8-way) gridLocs) when calculating boidage

5. More optimizations?
    a. Better use of rotation matrices, distances, pointers(?) 